/// <reference types="@testing-library/jest-dom" />

/**
 * Jest DOM类型声明
 *
 * 此文件扩展了Jest的Matchers接口，添加@testing-library/jest-dom提供的自定义matchers。
 * 这样TypeScript就能识别toBeInTheDocument(), toBeChecked()等自定义断言。
 */
