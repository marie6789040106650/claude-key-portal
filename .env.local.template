## Domain Configuration
NEXT_PUBLIC_DOMAIN="http://localhost:3000"

## Database - Supabase PostgreSQL
# Transaction Pooler (用于应用连接) - 必须使用 pgbouncer 模式
DATABASE_URL="postgresql://postgres.[PROJECT-ID]:[PASSWORD]@aws-1-us-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"

# Direct Connection (用于数据库迁移) - 不使用连接池
DIRECT_URL="postgresql://postgres.[PROJECT-ID]:[PASSWORD]@aws-1-us-west-1.pooler.supabase.com:5432/postgres"

# 获取连接字符串步骤：
# 1. 登录 https://supabase.com/dashboard
# 2. 选择项目 → Settings → Database
# 3. Connection string → Transaction (用于 DATABASE_URL)
# 4. Connection string → Direct (用于 DIRECT_URL)
# 5. 替换 [YOUR-PASSWORD] 为实际密码
#
# 注意：如果密码包含特殊字符，需要 URL 编码：
# node -e "console.log(encodeURIComponent('your-password'))"

## Redis - Local (可选，用于缓存)
REDIS_URL="redis://localhost:6379"

## CRS Integration
# CRS 基础 URL
CRS_BASE_URL="https://claude.just-play.fun"
# CRS 管理员凭据（用于自动登录获取session token）
# 浏览器登录页面: https://claude.just-play.fun/admin-next/login
CRS_ADMIN_USERNAME="cr_admin_4ce18cd2"
CRS_ADMIN_PASSWORD="HCTBMoiK3PZD0eDC"

## JWT Authentication
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="dev-secret-change-in-production"
NEXTAUTH_URL="http://localhost:3000"
JWT_SECRET="dev-jwt-secret-change-in-production"
JWT_EXPIRES_IN="24h"

## Application
NODE_ENV="development"
